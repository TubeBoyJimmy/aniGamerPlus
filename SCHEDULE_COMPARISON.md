# 智慧排程功能說明

> 本文件說明 aniGamerPlus 的智慧排程（Timer + Time Table）機制，與原始固定週期輪詢的差異比較，以及相關設定值。

---

## 一、兩種下載模式

aniGamerPlus 提供兩種更新檢查模式，由 `smart_schedule` 設定切換：

| | 固定週期輪詢 (`smart_schedule: false`) | 智慧排程 (`smart_schedule: true`) |
|---|---|---|
| **核心機制** | 每 N 分鐘檢查所有訂閱 | 根據排程表在播出時間精確觸發 |
| **觸發精度** | 分鐘級（受 `check_frequency` 限制） | 秒級 |
| **對巴哈的請求量** | 每輪 × 全部 SN | 僅觸發時刻 × 對應 SN |
| **適合情境** | 訂閱少、不在意精度、簡單好懂 | 訂閱多、追求準時下載、減少不必要請求 |

---

## 二、固定週期輪詢

最基礎的設計，不依賴排程資訊：

```
while True:
    對全部訂閱逐一檢查更新
    有新集 → 加入下載佇列
    sleep(check_frequency × 60 秒)
```

### 優點
- 邏輯簡單，不依賴巴哈首頁排程資料
- 適合少量訂閱（< 5 部），每次全查也不會有太多請求
- 不需要排程匹配，不會有匹配不到的問題

### 缺點
- SN 數量越多，每次輪詢的請求量越大（N 部 = N 次請求）
- 無法精確對準播出時間，最差延遲 = `check_frequency`
- 不必要的請求可能增加被巴哈封鎖的風險

### 適合的使用情境
- 只追 1-3 部番，不在意幾分鐘的延遲
- 不確定番劇是否在巴哈排程表上（如舊番、OVA、電影）
- 想要最簡單的設定，不需要排程相關參數

---

## 三、智慧排程 (Timer + Time Table)

**設計哲學：不輪詢、不睡眠、精確觸發。**

從巴哈動畫瘋首頁抓取每週排程表，建立時間表，以 1 秒為精度在播出時間觸發對應番劇的更新檢查。

### 運作流程

```
啟動:
    1. 抓取巴哈首頁排程表
    2. 對每個訂閱的 SN 匹配排程，算出觸發時間 (星期 + 時:分:秒)
    3. 預填今天已過的排程（避免啟動時誤觸發）
    4. 印出完整時間表

主迴圈 (每秒執行):
    ┌─ sn_list 變更？      → 立即重建時間表
    ├─ Dashboard 強制檢查？ → 立即檢查指定 SN
    ├─ 時間表比對:
    │   for 每個訂閱:
    │       if 今天是排程日 且 現在時間 ≧ 觸發時間 且 尚未觸發:
    │           → 檢查更新 + 下載
    ├─ 重試佇列:
    │   for 每個待重試項目:
    │       if 到達重試時間:
    │           → 重試 (10 分鐘 → 30 分鐘 → 60 分鐘 → 放棄)
    ├─ 全量 Fallback 檢查   ← 每 24 小時（預設）
    ├─ 排程表刷新           ← 每 1 小時
    └─ 觸發記錄週次重置     ← 每週一 00:00
    sleep(1 秒)
```

### 排程匹配

sn_list 中的 SN 不一定和排程表上的 SN 相同（例如追的是第 5 集的 SN，排程表上是第 1 集的 SN）。系統採用三層匹配：

| 優先順序 | 方法 | 說明 |
|---|---|---|
| 1 | SN 直接匹配 | sn_list 的 SN 剛好出現在排程表中 |
| 2 | 資料庫標題比對 | 用已下載過的 `anime_name` 去排程表中做子字串比對 |
| 3 | title_hints 比對 | 用 sn_list 的 Plex `clean_title`/`folder_name` 或 `rename` 去比對 |

標題比對規則（`_title_match`）：子字串長度需 ≧ 4 個字元，且佔較長字串的 40% 以上。避免短字串（如「AI」「K」）造成誤匹配。

### 時間表結構

```
啟動日誌範例:

排程模式 時間表已建立 (3 項):
sn=47063  排程  週六 00:00 → 00:00:30 (含延遲) 《和機器人啪啪啪...》
sn=47065  排程  週六 00:35 → 00:35:30 (含延遲) 《地獄模式...》
sn=46922  排程  週六 17:30 → 17:30:30 (含延遲) 《青之壬生浪 第二季》
排程模式 已略過 2 個今日已過時間的排程
```

- `00:00` = 巴哈排程表上的播出時間
- `→ 00:00:30` = 加上 `schedule_delay` 後的實際觸發時間
- 「已略過 N 個」= 程式啟動時已經過了播出時間的排程，不會重複觸發

### 觸發與重試

```
觸發成功:
  排程觸發 sn=46922  《青之壬生浪 第二季》播出時間到達 (17:30:30)
  更新資訊  添加了 1 個新任務

觸發但新集未上架:
  排程觸發 sn=47065  《地獄模式...》播出時間到達 (00:35:30)
  更新資訊  添加了 0 個新任務
  排程重試 sn=47065  《地獄模式...》未找到新集數, 10 分鐘後重試

重試成功:
  排程重試 sn=47065  《地獄模式...》第 1 次重試
  排程重試 sn=47065  《地獄模式...》重試成功, 已加入下載

重試放棄:
  排程重試 sn=47065  《地獄模式...》已達重試上限, 放棄
```

重試間隔: **10 分鐘 → 30 分鐘 → 60 分鐘 → 放棄**（共 3 次，總跨度 100 分鐘）

### Fallback 機制

不在排程表中的 SN（無法匹配或巴哈未列出）不會被時間表觸發。這些 SN 由定期全量檢查覆蓋，頻率由 `schedule_fallback_frequency` 控制（預設每 24 小時一次）。

其他安全機制：
- **每小時排程表刷新**：從巴哈首頁重新抓取，應對排程變動
- **Dashboard 強制檢查**：不受排程限制，1 秒內響應
- **sn_list 變更即時重建**：訂閱/取消時立即更新時間表
- **週次重置**：每週一 00:00 清空觸發記錄，讓下週排程正常觸發

### 優點
- 精確到秒，不浪費請求在「播出前」或「非播出日」
- 訂閱 20 部番，每天也只有 20 次觸發請求（而非每 5 分鐘 × 20 = 5760 次/天）
- Dashboard 操作即時響應，不需等待輪詢週期
- 日誌清楚標示每個觸發的時間和結果

### 缺點
- 依賴巴哈首頁排程資料，若首頁改版需更新解析邏輯
- 排程表外的番劇（OVA、電影、不定期更新）只能靠 fallback 檢查
- 匹配失敗時完全靜默（日誌不會有對應 SN 的任何輸出），需靠 fallback 補救
- 設定項較多，初次使用需理解排程概念

### 適合的使用情境
- 追番數量較多（≧ 5 部），希望減少不必要的請求
- 希望新集上架後第一時間下載（搭配 `schedule_delay` 微調）
- 長時間 24/7 運行的環境（如 NAS、miniserver）
- 使用 Plex/Emby 等媒體伺服器，需要檔案即時入庫

---

## 四、設定值參考

### 排程專用設定

| 設定值 | 型別 | 預設 | 說明 |
|---|---|---|---|
| `smart_schedule` | bool | `false` | 啟用智慧排程（Timer + Time Table） |
| `schedule_delay` | int (秒) | `0` | 觸發延遲補償。巴哈新集通常不會在整點秒上架，設 20-60 秒可避免觸發時新集尚未出現 |
| `schedule_fallback_frequency` | int (分鐘) | `1440` | 全量 Fallback 檢查間隔。針對無法匹配排程的 SN |

### 通用設定

| 設定值 | 型別 | 預設 | 排程模式影響 | 非排程模式影響 |
|---|---|---|---|---|
| `check_frequency` | int (分鐘) | `5` | 不使用 | 輪詢間隔 |
| `read_sn_list_when_checking_update` | bool | `true` | 觸發時重讀 sn_list | 每輪重讀 |
| `read_config_when_checking_update` | bool | `true` | 觸發時重讀 config | 每輪重讀 |
| `parse_sn_cd` | int (秒) | `5` | SN 解析冷卻 | 同左 |

### 已棄用設定

| 設定值 | 預設 | 說明 |
|---|---|---|
| `schedule_window_before` | `30` | 原窗口輪詢設計的「提前檢查」參數，現已不使用 |
| `schedule_window_after` | `120` | 原窗口輪詢設計的「檢查窗口」參數，現已不使用 |

### 建議配置

```jsonc
// 追番數量 ≧ 5，推薦啟用智慧排程
"smart_schedule": true,
"schedule_delay": 30,                // 延遲 30 秒觸發，避免搶在新集上架前
"schedule_fallback_frequency": 1440  // 24 小時全量檢查一次

// 追番數量 < 5，用固定輪詢即可
"smart_schedule": false,
"check_frequency": 5                 // 每 5 分鐘檢查一次
```

---

## 五、對巴哈伺服器的請求量估算

假設訂閱 10 部番，每部每週播出 1 集：

| 模式 | 每日請求量 | 計算方式 |
|---|---|---|
| 固定輪詢 (`check_frequency: 5`) | ~2,880 次 | 每 5 分鐘 × 10 SN × 24 小時 |
| 固定輪詢 (`check_frequency: 30`) | ~480 次 | 每 30 分鐘 × 10 SN × 24 小時 |
| 智慧排程（非播出日） | ~10 次 | Fallback 全量檢查 × 1 次/天 |
| 智慧排程（播出日） | ~13 次 | 觸發 + 最多 3 次重試 |
| 智慧排程（排程表刷新） | ~24 次 | 每小時 1 次首頁請求 |

智慧排程在一般日的總請求量約為固定輪詢的 **1%**。
